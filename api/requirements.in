fastapi
uvicorn
pydantic
# Market data (prices, macro) is read from the local Postgres filled by the
# `data-ingest` service — no cloud.
psycopg[binary]
psycopg-pool
pandas
numpy
scipy
requests
python-jose[cryptography]
bcrypt
# Optional cloud storage backend (QM_STORAGE=gcs). Uncomment to enable.
# google-cloud-storage
